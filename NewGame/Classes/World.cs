using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Content;
using Microsoft.Xna.Framework.Graphics;

namespace NewGame.Classes
{
    public class World
    {
        //private readonly TileMapEngine _worldMapEngine;
        //private Vector2 _previousCameraLocation;
        
        //public Viewport Viewport;

        //public World(ContentManager content, Viewport viewport)
        //{
        //    Viewport = viewport;
        //    _previousCameraLocation = new Vector2(-1000, -1000);
        //    _worldMapEngine = new TileMapEngine(content, viewport);
        //}

        //public void Update()
        //{
        //    if (_previousCameraLocation != Camera.Location)
        //    {
        //        _worldMapEngine.Update();
        //    }
        //}
        
        //public void Draw(SpriteBatch spriteBatch)
        //{
        //    if (_previousCameraLocation != Camera.Location)
        //    {
        //        _worldMapEngine.Draw(spriteBatch);

        //        _previousCameraLocation = Camera.Location;
        //    }
        //}
    }
}
